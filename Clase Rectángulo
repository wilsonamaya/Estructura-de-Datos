class rectangulo:
    def __init__(self, base, altura):
        self.b= base
        self.h= altura

B=float(input("Ingrese la base del rectangulo: ")) 
A=float(input("Ingrese la altura del rectangulo: " ))

c= rectangulo(B, A)

print("El area del rectangulo es: ", c.b*c.h)
